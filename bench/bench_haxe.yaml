lang: haxe
problems:
  - name: helloworld
    source:
      - 1.hx
  # - name: binarytrees
  #   source:
  #     - 1.hs
  # - name: pidigits
  #   source:
  #     - 4.hs
compiler_version_command: hl --version # hl is actually runtime here, haxe is compiler, just hack for now
compiler_version_regex:
runtime_version_parameter:
runtime_version_regex:
source_rename_to: App.hx
environments:
  - os: linux
    compiler: HL/JIT
    version: latest
    docker:
    include: haxe
    include_sub_dir:
    # before_build:
    build: haxe -m App --hl out/app.hl
    after_build:
      - ls -al out
      - cp ./run.sh ./out/run.sh
      - ls -al out
    out_dir: out
    run_cmd: run.sh
